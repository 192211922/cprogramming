#include<stdio.h>
int main()
{
	inta[10][10],r,c;
	printf("enter the rows and columns:");
	scanf("%d",&r,&c);
	printf("\n enter matrix elements:\n");
	for(int i=0;i<r;i++)
	for(int j=0;j<c;j++)
	{
		printf("enter element a%d%d:",i+1,j+1);
		scanf("%d",&a[i][j]);
	}
	printf("\n entered matrix:\n");
		for(int i=0;i<r;i++)
	for(int j=0;j<c;j++)
	{
		printf("%d",a[i][j]);
		if (j==c-1)
	
}
